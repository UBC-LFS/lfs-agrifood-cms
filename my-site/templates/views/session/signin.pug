extends ../../layouts/default

block content
	.container
		.page-intro
			h1 Sign In
		.page-content
			+auth-forms({ initial: 'signin' })

block js
	script.
		jQuery(function($) {
			$('.auth-form').find('.form-control').eq(0).focus().select();
		});
