extends ../../layouts/default

block content
	.containerWrapper: .container
		.page-intro
			h1 Sign into UBC Agrifood Database
		.page-content
			+auth-forms({ initial: 'signin' })

block js
	script.
		jQuery(function($) {
			$('.auth-form').find('.form-control').eq(0).focus().select();
		});
